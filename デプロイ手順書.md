# WalkPlanner 公開手順書

## 全体の流れ

```
あなたのPC → GitHub → Render（バックエンド） → Vercel（フロントエンド）
```

---

## ステップ1: 必要なアカウントを作る（全部無料）

1. **GitHub** → https://github.com/signup
2. **Render** → https://render.com （GitHubでサインアップ推奨）
3. **Vercel** → https://vercel.com （GitHubでサインアップ推奨）
4. **Anthropic** → https://console.anthropic.com （APIキー取得用）

---

## ステップ2: Anthropic APIキーを取得する

1. https://console.anthropic.com にログイン
2. 左メニュー「API Keys」→「Create Key」
3. 名前を入力して作成（例：walkplanner）
4. 表示されたキー（sk-ant-...）をメモ帳に控えておく

---

## ステップ3: GitHubにコードをアップロード

1. GitHub で「New repository」→ 名前：`walkplanner`→「Create」
2. PCでターミナル（Macはターミナル、WindowsはPowerShell）を開く
3. 以下のコマンドを実行：

```bash
cd walkplanner
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/あなたのユーザー名/walkplanner.git
git push -u origin main
```

---

## ステップ4: バックエンドをRenderにデプロイ

1. https://render.com にログイン
2. 「New +」→「Web Service」
3. GitHubリポジトリ「walkplanner」を選択
4. 設定を入力：
   - **Name**: walkplanner-api
   - **Root Directory**: backend
   - **Runtime**: Node
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Instance Type**: Free
5. 「Environment Variables」に以下を追加：
   - `ANTHROPIC_API_KEY` = （ステップ2で取得したキー）
   - `FRONTEND_URL` = （後でVercelのURLを入れる）
6. 「Create Web Service」をクリック
7. デプロイ完了後のURL（例：https://walkplanner-api.onrender.com）をメモ

---

## ステップ5: フロントエンドをVercelにデプロイ

1. https://vercel.com にログイン
2. 「New Project」→ GitHubリポジトリ「walkplanner」を選択
3. 設定を入力：
   - **Root Directory**: frontend
   - **Framework Preset**: Create React App
4. 「Environment Variables」に以下を追加：
   - `REACT_APP_API_URL` = （ステップ4でメモしたRenderのURL）
5. 「Deploy」をクリック
6. 完了！VercelのURL（例：https://walkplanner.vercel.app）で公開される

---

## ステップ6: バックエンドのFRONTEND_URLを更新

1. Renderのダッシュボードに戻る
2. walkplanner-api → Environment
3. `FRONTEND_URL` にVercelのURLを設定して保存

---

## ステップ7: Google AdSenseに申請（収益化）

1. https://adsense.google.com にアクセス
2. サイトのURLを登録（VercelのURL）
3. 審査（数日〜数週間）通過後：
   - `frontend/public/index.html` のAdSenseコードのコメントアウトを外す
   - `frontend/.env` に `REACT_APP_ADSENSE_CLIENT=ca-pub-あなたのID` を設定
   - GitHubにプッシュ → Vercelが自動でデプロイ

---

## 費用まとめ

| サービス | 費用 |
|---|---|
| GitHub | 無料 |
| Render（バックエンド） | 無料（15分無通信でスリープ） |
| Vercel（フロントエンド） | 無料 |
| OpenStreetMap（地図） | 無料 |
| Anthropic API | 使った分だけ（月数百〜数千円） |

※ Renderの無料プランはスリープがあります。常時起動したい場合は月$7のプランへ。

---

## アップデートのやり方

コードを変更したい場合：

```bash
git add .
git commit -m "変更内容のメモ"
git push
```

GitHubにプッシュするとVercelとRenderが自動でデプロイします。

---

## 困ったときは

- ビルドエラー → Vercel/RenderのLogsを確認
- APIが動かない → Renderのログ、APIキーの設定を確認
- 地図が表示されない → ブラウザのコンソール（F12）を確認
